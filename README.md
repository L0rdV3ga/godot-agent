# godot-agent

A Godot editor plugin that uses a local LLM to generate editor plans and execute actions.

Files included in this initial commit are plugin core scripts and example actions.

Usage:
- Install as an addon under res:// (copy the files/folders into your Godot project's res://)
- Open a scene in the editor, open the plugin panel, and issue a natural language command.
